# Core dependencies - matching notebook exactly
transformers>=4.43.3
peft>=0.11.1
accelerate>=0.33.0
datasets>=2.20.0
rapidfuzz
scikit-learn
tqdm

# Data processing (from notebook comment)
mido

# Standard libraries (usually pre-installed but listed for completeness)
pandas
numpy
torch
